import { Bid, Contribution, Finalization, StartPartyEvent } from "./types";

export const getCreations = async (
  fromBlock: number
): Promise<StartPartyEvent[]> => {
  // todo
};

export const getContributions = async (
  fromBlock: number
): Promise<Contribution[]> => {
  // todo
};

export const getBids = async (fromBlock: number): Promise<Bid[]> => {
  // todo
};

export const getFinalizations = async (
  fromBlock: number
): Promise<Finalization[]> => {
  // todo
};
